import barba from '~/js/features/barba'
import Vue from 'vue/dist/vue.esm'

// Mount vue components wherever you need them

barba.hooks.after(() => {
    // re-Mount the ones that are in barba containers
})
